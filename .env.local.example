# ─────────────────────────────────────────────────────────────
# 复制本文件为 .env.local 并填入你自己的密钥
#   cp .env.local.example .env.local
#
# .env.local 已被 .gitignore 忽略，不会提交到仓库。
# ─────────────────────────────────────────────────────────────

# 天地图（国家地理信息公共服务平台）地图服务密钥
#
# 申请步骤：
#   1. 打开 https://console.tianditu.gov.cn/ 注册并登录
#   2. 进入「应用管理」→「创建新应用」
#   3. 应用类型选「浏览器端」（服务端类型用于后端调用，此处不要选）
#   4. 提交后复制生成的 key，粘贴到下面
#
# 未配置时站点不会报错，会自动回落到自绘的示意底图（无真实地理形状）。
# 配置后刷新页面即可看到天地图矢量／地形晕渲／影像三种真实底图。
#
# 注意：前端密钥会被任何人从页面源码里看到，属正常现象（所有 Web 地图
# SDK 都是如此）。天地图按 key 统计调用量并设有额度，请勿把生产 key
# 提交到公开仓库以外的位置，也不要外借给他人。
NEXT_PUBLIC_TIANDITU_KEY=
