# 2026-09-25

## 移除 poetry-atlas 地图「未配置天地图密钥」提示条
- 用户要求：不要在地图上显示「当前为示意底图 / 未配置天地图密钥…」这段提示。
- `src/app/poetry-atlas/AtlasMap.tsx`：删掉 `hasKey === false` 分支里的 `<div className={styles.mapNoKey}>` 整块，
  该分支简化为直接渲染 `<SvgFallbackMap … />`（不再需要 Fragment 包裹）。
- `src/app/poetry-atlas/atlas.module.css`：删除配套的 `.mapNoKey` / `.mapNoKey strong` / `.mapNoKey code` 三条规则。
- 保留未动：`SvgFallbackMap.tsx` 的 `aria-label`（不可见）、`hasKey` 逻辑本身（仍决定走瓦片还是 SVG 底图）。
- 质量门禁：`npx tsc --noEmit` + `npx eslint src` 全绿；`CODEBUDDY_SAFE_DELETE_ENABLED=0 npm run build` 成功
  （126 静态页，trailing-slash 脚本复制 137 个 index.html）；`git status --short` 无意外删除；
  `grep -rl "当前为示意底图" out/` 已无命中。

### 顺带发现
- 仓库新增未跟踪目录 `.v/`（`全唐詩_卷539.txt`、`全唐詩_卷540.txt`，共 80K，9-19 生成），疑似诗歌文本快照，
  未经确认不动、不入库。加上原有的 `初中单词/`、`完型填空html/`、`public/quadratic-structure.drawio`、
  `poetry-atlas/_add_lisangyin.py`，未跟踪的用户资料清单又变长了。

## 用户提出「每次改完上传」的长期要求
- 用户原话：「帮我每次改完上传」→ 已写入 `MEMORY.md` 的「⭐ 用户要求」节：
  质量门禁 → 显式路径 add → commit → push master → 轮询 CI → curl 线上页校验，全程不再询问。
- 本次已执行：commit `0476df5` 推送成功，CI（Deploy Next.js site to Pages）**success**，
  线上 `https://zhuhairong.github.io/blog/poetry-atlas/` 返回 200 且已无「当前为示意底图」文案。
- 顺手校正了 MEMORY.md 里几处过期事实：地图已从「原型自绘 SVG」演进为
  「天地图瓦片 + SVG 回落」双轨；规模数字更新为 诗人 7 / 作品 112 / 地点 96 / 断言 122 / 静态页 126。

## 作品页逐句译注（提交 `1e47a2d`）
用户需求：「每一句诗词下面有小字解释」。先问清三点，用户选择：
**poetry-atlas 作品页** / **白话直译为主 + 难点加注** / **直接常驻显示**。

### 做法
1. 数据：`Work.lineNotes[]` 与 `content` 等长同序（`LineNote { trans, note? }`）。
   用平行数组而非嵌进 content——原文是公有领域语料，译注是编者自撰，版权属性不同。
2. 内容：646 行全部自撰，按诗人拆成 8 个批次用并行子 agent 生成，
   每批写一个 JSON 草稿，再用一次性脚本合并进 `works.json`（保留 CRLF + indent=2，
   实测 round-trip 字节一致，diff 纯新增 2591 行），合并后删草稿。
   结果 112 篇 / 646 行 / 424 条难点注。
3. 校验：`validate.ts` 加 `line-notes-length-mismatch`（error，平行数组错位极难肉眼发现）
   与 `missing-line-translation`（error），空行豁免；`line-notes-coverage`（info）。
4. 前端：正文改 `.poemLineGroup`，行距归属重排（`.poemLine` 2.1→1.75，
   组间距交给 group），译文无衬线二级灰、注更淡带「注」标签。
5. 文档：`poetry-atlas/README.md` 与技能 `poetry-atlas-add-poet` 同步补 lineNotes 要求。

### 边界情况
`黄州寒食诗二首` 第 4 行原文是空行（两首之间），`trans` 留空串、前端跳过，
不塞「此处无正文」之类占位文案。

### 验证
`atlas:build` 0 error / 0 warning（95 info）；`tsc + eslint` 全绿；`next build` 成功
（126 页）；构建产物里译文/注类名与文本均正确，空行处未渲染空块。

### 新掌握的能力（重要）
**本地能截图并读图**：起 `preview_atlas.py 4355` + headless chromium
（`C:/Users/86180/AppData/Local/ms-playwright/chromium-1208/chrome-win64/chrome.exe`）
截图，再用 Read 工具直接看 PNG。技能里「本产品读不了图片」的说法已过时并修正。
⚠️ 但窄屏截图不可信（布局宽度没收窄，媒体查询不生效），曾误判为横向溢出，
对比线上同宽度截图后才排除。


