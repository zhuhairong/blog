# 个人博客项目使用指南

High-performance personal blog built with Next.js and Markdown.

## 🚀 快速开始

### 1. 本地运行

```bash
npm run dev
```
打开 [http://localhost:3000](http://localhost:3000) 查看。

### 2. 发布与管理文章 (推荐方式)

**本项目已集成 GitHub 直接编辑功能：**

- **新建文章**: 点击首页的 **"+ Create New Post (GitHub)"** 按钮。
  - 这会跳转到 GitHub 的新建文件页面。
  - 请确保文件名以 `.md` 结尾（例如 `my-story.md`）。
  - 复制以下模板到内容框中：
    ```markdown
    ---
    title: "我的新文章"
    date: "2024-03-21"
    ---
    这里写内容...
    ```
  - 点击页面底部的 "Commit changes" 保存，网站会自动更新。

- **修改文章**: 在文章详情页，点击标题下方的 **"Edit this Post on GitHub"** 按钮即可跳转编辑。

- **手动添加**: 当然，你也可以像以前一样直接在 `posts/` 目录下操作文件。

### 3. 图片管理

将图片放入 `public/images/` 目录。在 Markdown 中使用：

```markdown
![图片描述](/images/filename.jpg)
```

或在 Frontmatter 中引用：
`coverImage: "/images/filename.jpg"`

## 🌐 部署到 GitHub Pages

1. 将本项目上传到 GitHub 仓库。
2. 进入仓库 **Settings** -> **Pages**。
3. 在 **Build and deployment** 部分:
   - Source 选择 **GitHub Actions**。
4. 推送代码到 `main` 分支，GitHub Actions 会自动构建并部署。

## 🎨 自定义样式

- 全局样式：`src/app/globals.css` (包含颜色变量、深色模式配置)。
- 页面布局：`src/app/layout.tsx`。
